#include <stdio.h>
#include "variadic_functions.h"

/**
 * sum_them_all - additionne tous les param√tres
 *
 * Return: 0 ou le r√sultat
 */

int sum_them_all(const unsigned int n, ...)
{
	unsigned int i;

	if (n == 0)
	{
		return (0);
	}
	for (i = 0; i < n; i++)
	{
		return (n);
	}
	return (0);
}
